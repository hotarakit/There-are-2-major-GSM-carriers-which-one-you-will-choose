$(".answer").click(function() {
    let container = $("provider").val();
    $(".output1").text("Using AT&T is very reccommended, since both premium plans are at the same price, you'll get more benefits if you use AT&T Unlimited Elite Plan which is better than the T-Mobile Magenta Max");
    console.log(container);
    
    
        


});
